# Règles projet

## Git

- Après chaque push sur la branche de feature, **toujours** pousser aussi sur
  `main` :
  ```
  git push origin <feature-branch>:main
  ```
  Demande du propriétaire — applicable même sans confirmation à chaque commit.
